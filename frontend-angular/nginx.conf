server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Esto hace que todas las rutas carguen index.html
        try_files $uri $uri/ /index.html;
    }
}